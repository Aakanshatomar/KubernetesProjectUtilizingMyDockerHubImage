apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodes-app-deployment
  namespace: notes-app
  labels:
    app: notes-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notes-app
  template:
    metadata:
      labels:
        app: notes-app
    spec:
      containers:
      - name: notes-app
        image: tomarakansha151/notes-app-k8s:latest
        ports:
        - containerPort: 8000
